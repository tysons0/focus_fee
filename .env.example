# API (root) - used by Vercel for /api/invest
# Copy to .env and fill in values

# Solana RPC (e.g. devnet)
SOLANA_RPC_URL=https://api.devnet.solana.com

# Treasury wallet secret key as JSON array (devnet)
SOL_TREASURY_SECRET=[1,2,3,...]

# Optional: mock SOL price in USD for conversion (default 100)
MOCK_SOL_PRICE_USD=100
