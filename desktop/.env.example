# Desktop app - copy to .env

# LOCAL: API running via `npm run dev` (local server on port 3000)
VITE_BACKEND_URL=http://localhost:3000

# PRODUCTION: after deploying to Vercel
# VITE_BACKEND_URL=https://focusfee.vercel.app
